apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qa-app.fullname" . }}-test-connection"
  labels:
    {{- include "qa-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qa-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
