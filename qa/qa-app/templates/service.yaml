apiVersion: v1
kind: Service
metadata:
  name: {{ include "qa-app.fullname" . }}
  labels:
    {{- include "qa-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qa-app.selectorLabels" . | nindent 4 }}
