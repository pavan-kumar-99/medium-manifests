apiVersion: v1
kind: Service
metadata:
  name: {{ include "staging-app.fullname" . }}
  labels:
    {{- include "staging-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "staging-app.selectorLabels" . | nindent 4 }}
