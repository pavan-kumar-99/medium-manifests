apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prod-app.fullname" . }}-test-connection"
  labels:
    {{- include "prod-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prod-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
